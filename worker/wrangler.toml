name = "roam-proxy"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
# Public (non-secret) config. The ElevenLabs voice id is safe to commit —
# override it with your own voice if you like. Secrets (ANTHROPIC_API_KEY,
# ELEVENLABS_API_KEY, ASSEMBLYAI_API_KEY) are set with `wrangler secret put`.
ELEVENLABS_VOICE_ID = "iP95p4xoKVk53GoZ742B"
